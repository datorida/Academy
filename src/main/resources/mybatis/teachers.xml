<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.academy.mapper.TeachersMapper">
    <select id="selectAll" resultType="Teachers">
        SELECT teachers_id as teachersId, teacher_img as teacherImg, teachers_name as teachersName,
        position, introduce FROM Teachers
    </select>
</mapper>