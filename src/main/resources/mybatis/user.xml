<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.academy.mapper.UserMapper">
    <select id="checkOverlapId" parameterType="String" resultType="Integer">
        SELECT COUNT(*) FROM user WHERE user_id = #{user_id}
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO USER
        (user_id, password, phone_number, address, email)
        VALUES(#{userId}, #{password}, #{phoneNumber}, #{address}, #{email})
    </insert>
    <select id="loginCheck" parameterType="User" resultType="User">
        SELECT user_id, password, phone_number, address, email
        FROM USER
        WHERE user_id = #{userId} AND password = #{password}
    </select>
</mapper>